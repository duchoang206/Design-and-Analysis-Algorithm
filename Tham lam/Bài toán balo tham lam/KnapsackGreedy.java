class KnapsackGreedy {

    /**
     * Bạn có một chiếc ba lô với sức chứa giới hạn (capacity).
     * Có n món đồ, mỗi món có khối lượng (weight) và giá trị (value).
     *
     * Mục tiêu:
     * Chọn các món đồ để tổng giá trị là lớn nhất, nhưng tổng khối lượng không vượt quá capacity.
     *
     * Ý tưởng tham lam:
     * Thay vì xét toàn bộ tổ hợp, ta chọn các món có tỷ lệ value/weight lớn nhất trước.
     *
     * @param weights mảng khối lượng
     * @param values  mảng giá trị
     * @param capacity sức chứa balo
     * @return tổng giá trị đạt được
     */
    public int knapsack(int[] weights, int[] values, int capacity) {
        // TODO
        return 0;
    }
}