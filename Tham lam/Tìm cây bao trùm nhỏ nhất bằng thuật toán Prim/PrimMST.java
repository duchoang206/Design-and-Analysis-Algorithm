class PrimMST {

    /**
     * Tìm cây khung nhỏ nhất và trả về mảng lưu các đỉnh cha của các nút cây
     *
     * @param graph ma trận kề (graph[i][j] = trọng số, 0 nếu không có cạnh)
     * @param n     số đỉnh
     * @return mảng parent của MST:
     *         - parent[i] là cha của i trong cây
     *         - parent[0] = -1 (gốc)
     */
    public int[] prim(int[][] graph, int n) {
        // TODO
        return new int[n];
    }
}